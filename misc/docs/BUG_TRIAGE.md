# Bug Triage - project config and state

Game-specific configuration and running state for the `game-bug-triage` skill.
The skill itself is game-independent; everything project-specific lives here.
Read this file at the start of every triage run.

## Game reference

- **BGA game id:** 2662 (Wayfarers of the South Tigris)
- **Bug list:** https://en.boardgamearena.com/bugs?game=2662
- **Single report:** https://en.boardgamearena.com/bug?id=<REPORT_ID>
- **Studio short name (for the error log):** `wayfarers`

## Rules sources (for `rules`-type reports)

Check a reporter's claim against these before assuming the code is wrong:

1. [RULES.md](RULES.md) - the rulebook.
2. [CHECKLIST.md](CHECKLIST.md) - implementation checklist / clarifications.

(No FORUM.md in this project yet; add one here if designer rulings get saved.)

## Deploy model

Process: **creating a git tag = deployed.** To decide if a fix is live:

- Find the fix commit: `git log --oneline -i --grep='#<reportid>'` (commit messages reference `BGA #<id>`).
- Is it in the latest tag: `git merge-base --is-ancestor <commit> <latest-tag> && echo LIVE`.
- List tags containing it: `git tag --contains <commit> --sort=creatordate | head`.

In the latest deployed tag -> **Fixed**. Committed/tagged but that tag not deployed -> **Waiting for deploy**.

To confirm what is actually live (and avoid guessing whether a tag is deployed), open the studio manage-game page: https://studio.boardgamearena.com/studiogame?game=wayfarers - the "Online version" field shows the current production version string, and the "Versions available for production" list marks one entry "(Current PRODUCTION version)" with its release-note summary. Match a report's fix to that release note to confirm it shipped. (This is the authoritative deploy check; the git-tag steps above are the local proxy.)

## Investigation and tests

- Tests live under `phptests/` (flat, mirroring op/source names, e.g. `Op_rerollTest.php`).
- A reproducing test must be **green**, asserting the current buggy behavior, with a comment pinning it for `BGA #<id>` (flip the assertion when the fix lands) - see the skill's CONFIRMED note.

## Bug triage last checked

2026-09-02 17:08 EDT

(View verified current this run: #242026 created 9/2 14:50 showed as "2 hours ago", #240391 created 8/25 as "8 days ago", #236323 created 8/5 as "29 days ago".)

## Tracked bugs

Internal record of triaged reports (root cause, fix, test) - never put this detail in a public bug comment.

- [ ] BGA #231680 (display, mobile) - CONFIRMED 2026-08-22, root cause measured. The first Land/Water card in the panorama sits 3.39px lower than every following card, so the row breaks at the board/first-card junction only; cards 2..4 align with each other exactly. Cause: Game.ts:722 special-cases `x == 1` and parents the first card straight into `pboard_{color}` instead of a `pboard_column_1_{color}` wrapper like its siblings. Land/water cards are anchored `bottom: 8.5%` of their own container (Cards.scss:144), and the two containers are not the same height - `.pboard` takes `aspect-ratio: 798/1024` (Boards.scss:155), which at width 376.95px renders 483.70px, while `.column` uses the fixed `$pboardHeight: calc($cardHeight * 2.4)` = 480px (\_variables.scss:67). The 3.70px height gap times (1 - 0.085) = 3.39px, exactly the measured offset. MEASURED in the real client, not inferred: harness snapshot with 4 water cards forced into `tableau_6cd0f6`, geometry read via headless Chrome - card 1 top 341.59, cards 2/3/4 top 338.20, board's own pre-printed `card_home_*` row top 344.02 (a third baseline, `.card_home { bottom: 8% }`). No repo test: jsdom does no layout, so `npm run jstests` cannot assert this - reproduce with `npm run play` plus a DOM measurement. Two fix directions, maintainer's call: give the first card its own `pboard_column_1_*` wrapper so all cards share one containing block (but `.pboard .card[data-state="1"] { right: 0% }` may be a deliberate tuck against the board edge), or derive `$pboardHeight` from the same 798/1024 ratio so the two containers agree.
- [ ] BGA #242026 (rules, solo) - I think its misinterpretation of the rules. Asking designer.
- [ ] BGA #240391 (action, solo) -Not a bug
- [ ] BGA #224966 (rules, 3 votes) - INFO NEEDED, investigated 2026-09-02 against the second reporter's pattern, UNCONFIRMED. Claim: black influence gained mid-turn (townsfolk ability, or income/moving influence while resting) is not honoured for the Science Guild extra Journal space.
  - RULED OUT: the per-turn flag is reset on every real turn - `used_inf_black_{owner}` is zeroed at Op_turn.php:51, and `auto()` only skips that branch for a mid-turn `reentry` (Op_turn.php:28-31, :201) or for PLAYER_AUTOMA. Also ruled out: stale evaluation at queue time. Nothing prunes void ops at queue time; `OpMachine::insert()` (OpMachine.php:208-215) inserts at rank 1 so the offer always runs before turn confirmation, and `hasBlackInfluence()` counts live tokens. A green test drives the exact reported sequence - phptests/Op_spendInfBlackTest.php, `testOfferedWhenBlackInfluenceGainedDuringTheTurn` and `testFirstJournalMoveQueuesTheOfferAfterMidTurnGain`: 0 black at turn start, gained mid-turn, offer appears.
  - A RULES-CORRECT EXPLANATION THAT FITS "not always": from jpos_40 and jpos_47 the ONLY exit costs 1 Black Influence on side A (`40|50|Op(n_infBlack)`, `47|55|Op(n_infBlack)`; side B charges Blue+Yellow instead), and Op_journal.php:117-119 pays the splotch before Op_journal.php:132 offers the Guild move. A player holding exactly one Black spends it crossing, so there is correctly nothing left for the extra space. RULES.md "Journaling": "Some ink splotches require players to spend Influence. If they do not have the Influence to spend, they cannot move past." Pinned by `testCrossingAPaidSplotchConsumesTheOnlyBlackInfluence`.
  - SEPARATE LEAD FOUND ALONG THE WAY (unverified, no test): Op_spendInfBlack.php:48-51 pre-checks `journal->noValidTargets()` BEFORE the Guild cost is paid, then resolve() spends the influence and queues the journal. From jpos_40 holding one Black, the offer therefore appears, the influence is spent on the Guild ability, and the only onward connector is then unpayable - the player pays for a move they cannot make. Worth checking independently of this report.
  - REMAINING LIVE HYPOTHESIS (needs a campaign test, not written): the Rest path. Op_rest.php:76-87 wraps rest-ability cards in an Op_order whose delegates are all instantiated up front; if a `journal` delegate's args (and `Operation::$cachedArgs`) are computed before an influence-granting delegate resolves, the offer would be decided on stale state. Scenario to write: rest with two rest abilities, one granting Black Influence and one granting a journal, resolved in that order.
  - ASK THE REPORTERS if it stays open: which marker position (40/47/90/95 are the ones whose exit costs Black); did the first journal step itself cross a Black-cost splotch; had the Science Guild already been used that turn; and move numbers from table 865081814.
- [ ] AUTO ERROR (no report) - `structuredClone is not defined`, studio id 621298 (JS-9PWG), 5 events / 1 user, 2026-08-05, live release. Culprit `Game.getTokenDisplayInfo` (Game1Tokens.ts:630; also used at Game1Tokens.ts:239) on Chrome Mobile 91 / Android 10 (Huawei AQM-LX1 - a vendor-frozen device, not a user choice). Impact is a hard block for that user, not cosmetic: the throw propagates up `onEnteringState_PlayerTurn` so the turn's buttons never render. Low reach; flagged rather than actioned since it is squarely the old-browser tradeoff - `structuredClone` here only deep-copies a plain material object, so a spread or JSON round-trip would remove the dependency if you decide it is worth it.
- [ ] AUTO ERROR (no report) - `action_undo method is not defined in this game`, studio id 631258, 2 events / 2 users, 2026-08-08 and 2026-08-18, live release. `GameMachine.ts:438` posts `action_undo` with `checkAction: false`, so the framework does not verify the action is legal in the current state and the server throws when the state class has no `action_undo`. Only PlayerTurn and PlayerTurnConfirm define it; EndScore / GameDispatch / GameDispatchForced / MachineHalted do not, so an Undo click that lands after the state has moved on gives the player a red "server reported an error" banner. Low volume, but it is a real player-visible error. NOT the cause of #238488 - that report's table has no server-side error record at all.
- [ ] AUTO ERROR (no report) - `UserException: Cannot skip this action`, studio id 643322, 1 event, 2026-08-16, live release (also seen once on 260716-1846, so long-standing and rare). `action_resolve {"target":"confirm"}` -> `action_whatever` -> `action_skip` -> `skip()` refuses at Operation.php:708. A controlled refusal shown to the player rather than a crash, so log noise, but it means the client offered a confirm/skip the server would not accept.

## Open, not investigated

One-line index of reports carried across sweeps without being opened. Titles pulled from the
tracker 2026-08-24 so a future pass can prioritise without loading each page. Most are parked in
`Info needed` awaiting a reporter reply, which is why they keep surviving sweeps.

- [ ] BGA #236323 (access) - OPEN 2026-08-05, no detail to act on. Reporter saw "BGA Service Error" and says refreshing does not help; Edge 151 on Windows. No table state or move given. Same shape as #233565, likely platform rather than game, but unconfirmed.
- [ ] BGA #224966 (rules, 3 votes) - INFO NEEDED since 2026-07-25, but NO LONGER STALE: a second player (Jabbajose) answered on 2026-08-25 with a concrete pattern - "I didn't have a black influence to start the turn / I then gain black influence during the turn prior to the journaling (either townsfolk ability or income (includes moving influence) when resting) / But then no option shows up for 2nd movement". That is a testable claim, so this is now the most actionable old report on the list. Original table 865081814, first reporter writes in French. Investigated 2026-09-02 - see its entry under Tracked bugs.
- [ ] BGA #228863 (display, iPhone) - INFO NEEDED since 2026-07-16, reporter has not replied. Pinch zoom snaps straight back to the original view on iPhone (Safari 26.5, iOS 18.7). We answered that this is scale-to-fit mode and the zoom button unsnaps it. Table 875971667. Same subsystem as #233129, so the LaZoom port would likely settle both.
- [ ] BGA #218672 (rules) - INFO NEEDED, stalled 2026-05-12 on a design decision, not on missing information. Yellow Influence (modify dice) can currently only be spent while selecting a die; the reporter wants it usable after the main action too, since holding one use over to the next turn is sometimes the only way to shift a die 3+ pips. A pre-main-action button was added; the open question is whether to also surface it at turn confirmation, which we pushed back on as disturbing the flow for a rare case. Effectively a deferred feature request. Table 845402503.
- [ ] BGA #217089 (rules) - INFO NEEDED since 2026-04-22, reporter has not replied. Using a caravan minus-tile to change a die from 4 to 3 should commit the player to that die, but the game still lets them switch to a different one (it does correctly block switching to a worker). We replied "press undo", which does not address the rules point. Table 840474789, move 130. Probably a real enforcement gap and worth a second look rather than another info request.

## Postponed

- [ ] Wording (left over from the #227997 fix, shipped in 260824-2124) - journal spots 20/23/27/60/63/67 now reward `newDie,cardInsp`, but their tooltip still reads "Acquire Inspiration Card and Gain Die", i.e. the old order. Left as is deliberately to avoid churning the existing translation; flip the wording if you would rather the tooltip match the resolution order.

- [ ] BGA #240682 (typed `suggestion`, so no triage action taken) - "I placed a six on the card/tile spot, but didn't have sufficient coin, so nothing happened", solo, table 906003811, move 32, confirmations off so the die was spent with no undo. Left as a suggestion per the workflow, but it looks adjacent to real code rather than to a feature: the dead-end veto from the #227894 / #239757 work refuses a die slot whose action cannot be paid, yet this slot (Space Card OR Space Upgrade) apparently offered itself and then resolved to nothing. HYPOTHESIS, no test, not investigated: the slot's operation is an Op_or, and Op_or already auto-prunes void branches silently (see the tech-debt note below) - if every branch prunes, the veto may see a non-void OR and let the placement through. Worth a look next time the veto is touched.
- [ ] Data (found while investigating #240391, harmless today) - in journal_material.csv the side-B block puts `#set location=mainboard_3` before row `72|80`, while side A puts it before `90|100`. So side-B connectors 72_80 through 87_95 claim `mainboard_3` even though positions 72-87 are physically board part 2. Nothing reads it today (`getConnectorId` always takes the side-A row's location), but it is wrong data and would bite anything that renders or groups connectors by their own location.
- [ ] UX (surfaced by #233367, general) - the common OR operation (Op_or, see the isVoid() filtering around Op_or.php:119-125) silently auto-removes a branch when it is void, so the player just sees fewer options with no explanation (e.g. the "buy Space Card OR Space Upgrade" choice collapsing to only the upgrade when no Space-Card slot is available). Consider logging when a branch is auto-pruned as void, so the player understands why an expected option is missing. General fix in Op_or, not specific to cardSpace. Same flavor as the automa missing-reason work that shipped in v260726-1447.
- [ ] Tech debt (surfaced while fixing #229172, now closed) - the wider class of bug is NOT fixed, only that one instance. `CountableOperation::getIconicName` still yields the template `${name} x ${count}` when the op has no `wicon` in the material and does not override the method; `cardDraw` is the only op type that actually reaches it today (29 op types lack a wicon, but the rest either override getIconicName and interpolate, or extend plain Operation, whose getIconicName returns the bare op name). Consumers that drop the args when embedding a name: Op_upgBase.php:204 (into a prompt), Op_placeDie.php:139, Op_turn.php:133. Joiners that make per-sub args unattachable in principle: Op_or.php:106, Op_seq.php:82, Op_order.php:86, Op_paygain.php:37. Nothing is on fire, but the contract "an iconic name may contain placeholders, and its args must ride along" is unwritten and violated by 7 call sites - embedding `cardDraw`'s name anywhere, or giving any other CountableOperation subclass a placeholder name, reintroduces the bug. Decide whether iconic names should be contractually placeholder-free.

- [ ] BGA #233129 (action/display, mobile) - the zoom controls (.board_layout_controls, Boards.scss:538) are absolutely positioned at the tableau's left edge (left: 8px, z-index 900) and overlap the far-left upgrade tile / far-left card, worst on narrow mobile. Kept OPEN, nothing posted. There is NO way to hide them today (only gamepref #101 confirm-turn; localStorage stores zoom mode/scale/journal-collapse only). FIX: add a local setting to hide the zoom controls, copying it from the fate game - port fate/src/LaZoom.ts (reusable zoom module; its "Scale to fit" mode hides board_layout_controls via the controls_visible toggle, LaZoom.ts:96-101) + fate/src/LocalSettings.ts, wiring the choice as in fate/src/Game.ts:262 ("Scale to fit" vs "Show zoom controls"). Wayfarers currently has zoom code inline in Game.ts; porting LaZoom gives the hide-controls setting.

## Triage run log

Short log of each run: date, reports touched, outcome.

- 2026-07-23 - First run (skill test, single report). #233581: not already-fixed (no fix commit; report postdates latest tag v260716-1846). Two agent hypotheses (Op_turn marker reset; cardFolk-not-offered) were both DISPROVEN against the actual dump (studio table 928069). Real cause found by tracing the op-machine and confirmed by a Campaign integration test: automa acts mid-human-action via cardInteract -> ai_res -> 4.5 r2 = ai_cardFolk acquisition. Report still OPEN, no public comment posted. Single report only - NOT a full sweep, marker intentionally left unset.
- 2026-07-24 - #231682 (guild influence click) looked at, left OPEN unchanged (likely influence-token intercepting the tap; skipped per maintainer). #233367 (purchase space card) triaged -> NOT A BUG, player-facing rule comment posted, status flipped; secondary UX note tracked (silent void option). #233129 (zoom controls overlap) triaged -> real client-side UI overlap, kept OPEN, tracked with a note to port fate's LaZoom + LocalSettings for a hide-zoom-controls setting. Still not a full sweep, marker still unset.
- 2026-07-24 - Waiting-for-deploy sweep. Confirmed production version 260716-1846 is live via the studio manage-game page (release notes: caravan-full upgrade fix, solo scheme-card tooltips, translatable strings). All 4 waiting reports were marked "fixed on my side, next release" on 2026-07-16 (18:22-18:42), just before the 18:42 tag, and only non-fix commits sit after the tag -> all shipped. Flipped to FIXED (template comment posted, verified): #227567 (pink tile / caravan full), #230012 (missing translation), #230002 (solo scheme-card action description), #229078 (townfolk popup / playerboard). Waiting-for-deploy queue now empty.
- 2026-07-25 - Remaining-Open pass. #233565 (game not loading = 504 Gateway Time-out) = BGA platform/infra issue, not a game bug -> left for maintainer to handle. #228863 (iPhone can't zoom) = Info needed, dev reply already given, no reporter response -> left as-is (same zoom family as #233129: fit-mode resize listener likely re-snaps on pinch). #231680 (cards not aligned) = reporter HAD answered the info request with 2 images (Jul 16); reviewed them and could NOT see a real misalignment -> posted a clarifying comment asking if it's a few-pixel offset or something else, left Info needed. #231682 and #233129 remain OPEN with their notes.
- 2026-07-25 - Full Open-list pass (filtered by bug statuses, 17 total). Confirmed 4 bugs, each with a reproducing test written by an investigation agent and verified green, then flipped to Confirmed on the tracker with the template comment: #229172 (discount-to-0 ${count} substitution error; test Op_upgGreenDiscountZeroTest; cause Op_upgGreen::getPaymentOperation missing zero-guard), #229079 (placed upgrade tile shows background cell tooltip; JS test src/tests/UpgradeTooltip.spec.ts; cause cell tooltip not cleared when covered), #227894 (SOFTLOCK: interacting with an unaffordable influenced card, no skip, undo blocked; test Campaign_CardInteractCannotPaySoftlockTest; also posted English translation of the French report), #227997 (inspiration tuck not undoable; test Campaign_InspTuckUndoTest; cause Op_cardInsp registers no undo savepoint - harness caveat noted). #230035 (dice reroll) -> Works-for-me (reporter self-retracted; a reroll can repeat a face). #230000 (scheme comet flags) recorded as an OPEN lead needing the maintainer to confirm which cards' comet values are wrong. Then finished the remaining: #223536 (mobile popup too wide, X offscreen) -> FIXED (git-verified: 99aabac "phone-safe dialog/tooltip widths, backdrop-close", committed 34 min after the maintainer's own comment on this report, in the deployed tag; posted a note that you can tap outside to close). #224966 (rules: black-influence extra Journal space "not always possible", French, vague) -> Info needed, asked for the specific move (also noted the extra space still needs the connection requirement + is once/turn). #221524 (rules: forced ordering of a Water card's simultaneous immediate effects blocks a legal combo) -> recorded as OPEN lead. #218672 (yellow influence after main action) and #217089 (die-switch after caravan pip change) -> both Info-needed with the maintainer's own last word and no reporter reply since (months old); left as-is - maintainer's call to close/won't-fix. FULL OPEN-BUG LIST NOW TRIAGED.
- 2026-07-27 - Targeted incident sweep for the solo final-scoring crash (NOT a full sweep; only reports tied to this one error were touched). `EndScore::finalScoring` read the lost-solo player as `$players[0]`, but `Player::fromPlayersDb` keys by player_id, so `GameResult::solo` got null and the whole confirm was rolled back: the player could not finish, retried, and the table sat blocked. Shipped in 260726-1447 (from a9d8b5d, 2026-07-23) and cost 319 events across 17 tables / 10+ users in ~17 h. Fixed by e6c068b (v260727-0834), which then regressed: `loadPlayersBasicInfos` carries no `player_score`, the real `Player::fromPlayerDb` reads that key directly (the vendored stub guards it with `isset()`, so NO local test can catch this class of bug), and `exception_on_warning` promoted the warning to a fatal - same softlock, 21 events / 3 tables over ~12 min. Hotfixed by 6b857d7 (v260727-0846): inject `player_score` / `player_score_aux` onto the row before building the Player, same pattern as bga-scholars Game.php:911. `exception_on_warning` is commented out in gameinfos.inc.php because the deploy checker rejects assigning those fields - re-enabling it needs the Player built via its constructor instead. Verified after deploy: zero events on 260727-0846. Flipped to FIXED with a player-facing comment (proven by matching each report's table id against the error's table breakdown): #234465 (889324293), #234506 (889510080), #234482 (889448556 - titled "rerolling a die" but the reporter's own text says "just end the game"), #234460 (889338903), #234519 (889512666, Polish: "when confirming my action, in the last round of the game ... every time regardless of the action chosen"), #234481 (889436555). Also #234443 (889288450) - same symptom, solo, final turn, filed 49 min after the crash appeared, but its table has NO server-side error record; closed as Fixed on the maintainer's call, reporter said they abandoned the game.
- 2026-07-27 (13:52) - Full unresolved-list pass (16 reports), first run to set the marker. Deploy sweep against v260727-1346: three reports whose fixes had shipped in v260726-1447 but were never flipped went to FIXED with a template comment (verified after reload) - #229172 (discount-to-0, fix 94c4b11), #230000 (scheme comet flags, fix 1f68d6c), #233581 (automa acting mid-action, fix 67e921d); their tracked-bug entries were removed, and the standalone iconic-name placeholder note from #229172 was kept as its own tech-debt item. #227997 / #227894 / #229079 stay CONFIRMED: the only commits naming them add repro tests, no fix has landed. #234349 (no description for land 9) triaged for the first time - proven to be a malformed row in cardland_material.csv, left OPEN pending the one-character fix. Verified today's two incidents are dead on the live release (solo GameResult crash last seen 06:57 on 260726-1447; player_score warning last seen 09:03 on 260727-0834). The JS performAction `.then` family shows no events since the 13:46 deploy, but that is only ~6 min of exposure, so it is unproven rather than confirmed. Untouched, unchanged since the 07-25 pass: #233565, #233129, #228863, #231682, #231680, #224966, #221524, #218672, #217089.
- 2026-08-22 (12:28) - Full unresolved-list pass (31 reports, of which 15 are already-classified `suggestion` rows left alone). NO DEPLOY since the last run: the studio manage-game page still shows production 260727-1346, and 9 commits sit after the v260727-1346 tag - so the Waiting-for-deploy sweep was a no-op (queue is empty) and nothing could be promoted to Fixed. Three new/changed reports triaged: #236981 (German, solo, "AI card executed wrongly") -> NOT A BUG - the AI placed its blue Worker on the Water space whose printed actions are `reroll,diceMod,diceMod`, and per RULES.md:374 "They resolve all printed actions of a space when placing a Worker" plus RULES.md:334 (AI board icon conversions) those became retrieve-a-Worker + two place-Influence-on-a-Card, which is the Influence the reporter saw on #65/#67; conversions verified at Op_reroll.php:23 and Op_diceMod.php:23; decisive tell - the card's second action places Influence in GUILDS, not on cards. Posted an English translation of the report first, then the rules explanation. #231680 (cards not aligned) -> CONFIRMED - a second reporter (pistolstar, 2026-08-17) finally pinned it to the board/first-card junction; measured 3.39px in the real client via the harness, root cause and both fix directions in the tracked-bug entry above. #235024 (scoring) -> INFO NEEDED - claim 1 (primary tags "10 not 16") answered from RULES.md Key Concepts > Tags, which scores primary tags in two separate lines (common sets, then 5VP per unique set of 4), matching Bender3002's 10+5=15; claim 2 (Collector's Star #104 short by 3) cannot be checked without the reporter's final card counts, so the report stays open on that half. #236323 ("BGA Service Error") left OPEN - platform/infra, not a game bug: the report carries no dump ("0 player, dump game state: null") and its table has NO server-side error record over three months, same family as #233565; maintainer's call to close. #238488 (Japanese, "cannot execute the Undo button") was already closed Won't fix by the maintainer on 2026-08-21 under the English-only policy, so it was left untouched and no comment was posted; for the record its table likewise has NO server-side error record, so it is NOT the `action_undo` exception below - translation: "Undo" / triggered by "restart" / observed "displays none", plus "I reloaded and restarted again, but it did not work" (iPhone iOS 18.7). Error-log sweep over the last 30 days recorded 4 auto errors above, and CONFIRMED the fix the 07-27 run could only call unproven: the JS `performAction .then` family (11 issue ids, ~178 events) has had ZERO events since the 260727-1346 deploy, ~26 days of exposure. The solo GameResult crash and the `player_score` warning are also still dead. Untouched, unchanged: #233565, #233129, #228863, #231682, #224966, #221524, #218672, #217089, and #234349 / #227894 / #229079 / #227997 (see the open question in the tracked-bug entries about flipping the three fixed-in-code reports to Waiting-for-deploy).
- 2026-08-23 (20:10) - Deploy sweep after v260823-2009 (first tag containing these fixes; production confirmed by the tag and the changelog entry). Flipped to FIXED with player-facing comments, each verified after reload: #221524 (effect ordering, fix 2521a1e), #227894 (cannot-pay softlock, fix 0022398), #229079 (upgrade tile tooltip + mobile width, fix e681841), #234349 (land 9 description, fix 6b8e28d); their tracked entries were removed. #227997 deliberately NOT flipped - the only commit naming it is the repro test 6ccd4a1, tagged since v260726-1447, and no fix exists, so it stays CONFIRMED. Rules cited this run for the two that needed it: #221524 against "a player may then perform the actions of that space, in any order they choose" (Player Turns > Placing a Die) plus "Townsfolk abilities can be resolved before or after the Land or Water Card's ability" (Cards > Townsfolk Cards); #227894 against "If they cannot pay, then they cannot interact with that Card" (Key Concepts > Influence). Pre-flip regression check on the new release: no PHP errors at all since the deploy and nothing new on 260823-2009 - only known noise (third-party module loads, JS-TY / JS-2C8) and the two already-tracked auto errors, so no repeat of the 07-27 ship-and-regress. The `& > *` selector bug is still live at Game.ts:666 - NOT fixed in this deploy. One new report since the marker: #239548 (AI resource marker, solo) triaged and left OPEN with an unresolved rules question, no comment posted - see its tracked entry. #235024 and #231680 showed as changed but only from our own 08-22 actions, so skipped per the marker rule. Untouched, unchanged: #236323, #233565, #233129, #228863, #231682, #224966, #218672, #217089.
- 2026-08-24 - Follow-up on #239548 only (not a sweep, nothing posted, report still OPEN pending deploy). Settled the open rules question from the board art shipped in this repo rather than from the reporter's video: the AI Board conversion is per unit, so the reported behaviour was a real bug. Fixed Op_gain to pass the gain count to `ai_res` and added phptests/Op_gainTest.php; full suite green (343 tests). Flip to FIXED on the next deploy sweep, once a tag containing the fix is live. Also fixed the `& > *` auto error the same day (Game.ts:666 now `:scope > *`); it has no bug report to flip, so it just needs the deploy.
- 2026-08-24 (21:25) - Deploy sweep after v260824-2121, live as production 260824-2124 (confirmed on the studio manage-game page: "Online version 260824-2124", marked "(Current PRODUCTION version)", release notes matching all four fixes). Flipped to FIXED with player-facing comments, each verified after reload: #239757 (tucked Townsfolk not counted when vetoing a die slot, fix 773e728, 21:28), #239548 (AI resource marker per unit, fix 7573244, 21:30), #227997 (inspiration tuck undo, fix a0b6574, 21:33). The `& > *` selector auto error also shipped (8d820aa) and its tracked entry is removed - no report existed to flip, and its last studio event is 8/21, before the fix. Pre-flip regression check on the new release: this week's error list holds only known noise (JS-TY and JS-2C8 third-party module loads) plus the already-tracked `action_undo` PHP error last seen 8/18 - nothing attributable to 260824-2124. CAVEAT: the deploy was only minutes old at check time, so the clean log is weak evidence; re-check next run. #239757 was found, root-caused, tested and fixed within this same day - see its removed entry in the 08-24 follow-up line below for the diagnosis. Already-fixed grep run for all 10 remaining unresolved reports: no fix commits (the #231680 and #233129 hits are docs commits from earlier triage passes, not code). Untouched, unchanged: #235024, #231680, #236323, #224966, #233565, #233129, #228863, #231682, #218672, #217089.
- 2026-09-02 (17:08) - Full sweep. TWO reports were updated: #240391 -> Info needed with a player-facing question (17:30), and #242026 -> Confirmed (17:44), both posted and verified after reload. The first attempt was refused by the session's permission classifier and went through on a retry; #224966 was deliberately NOT posted, still awaiting a decision. Deploy sweep is a no-op - the studio manage-game page still shows Online version 260824-2124, no tag after v260824-2121, and the Waiting-for-deploy filter returns 0 reports. Open filter holds 6: #242026, #240391 (both new and both investigated this run; #240391 has since moved to Info needed), plus the four long-stale #236323, #233565, #233129, #231682, none of which changed (29-50 days) and all of which keep their existing notes. Five rows had moved since the marker: #242026 (new), #240391 (new), #240682 (new, typed `suggestion` - reviewed, no action, recorded under Postponed because it looks adjacent to the die-slot veto rather than to a feature), #224966 (a SECOND reporter finally answered the July info request with a concrete pattern), and #239548 (only a "Thank you" from the reporter, stays Fixed). Three investigations ran, all with tests left green in the tree: #242026 -> triage first read the rule the other way and argued the code was right; the maintainer ruled that the middle option is the middle SPACE of the final column (103, with 100/107 the ends), which makes the reporter right, so it was flipped to Confirmed (phptests/Op_ai_journalTest.php, 11 green, now pinning the buggy 103); #240391 -> the tag-count theory is DISPROVEN, caravan and pink-tile tags do count and the comparison is `>=` (phptests/Op_journalTest.php, 38 green), leaving the maintainer's own occupancy hypothesis, which RULES.md:106 supports even in solo; #224966 -> UNCONFIRMED, the per-turn flag and the live influence count are both fine (phptests/Op_spendInfBlackTest.php, 5 green), but a rules-correct explanation for "not always" was found (from jpos_40/47 the only exit costs the same Black Influence) plus a separate unverified lead in Op_spendInfBlack's pre-check ordering. Error-log sweep over 30 days RESOLVES the caveat left by the 08-24 run: every game-code error - both `& > *` ids, structuredClone, action_undo, Cannot skip - was last seen on or before 8/21, so nine days of exposure on 260824-2124 produced nothing but platform noise (envoy file_get_contents, bga-animations module import JS-TY / JS-2C8). The `& > *` fix is confirmed dead. Tables for #242026 and #240391 both report "No errors!", so neither is a server-side crash. Untouched, unchanged: #235024, #231680, #236323, #233565, #233129, #228863, #231682, #218672, #217089.
